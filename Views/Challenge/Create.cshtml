@model Project.Models.ValidModels.CreateChellange

@{
    Layout = "_Layout";
}

<section>
    <div class="container">
        <div class="col-12 text-center">
            <h4>Ваша собственная мастерская</h4>
            <p class="text-muted">
                Прежде чем создать задание, убедитесь в его корректности
            </p>
        </div>
        <section>
            <div class="col-12 justify-content-center mt-2">
                <div class="container justify-content-center d-flex bg-white border rounded p-2">
                    <form class="col-12 bg-light p-2" asp-action="create" asp-controller="challenge" method="post">
                        <div class="col-12 d-flex">
                            <div class="form-group col-6 pe-2">
                                <p class="mb-0"><kbd>Название</kbd></p>
                                <input asp-for="Name"
                                       type="text"
                                       class="form-control form-control-sm"
                                       id="title"
                                       placeholder="Введите название" />
                                <p class="mt-2 mb-0">
                                    <kbd>Описание</kbd>
                                </p>
                                <textarea asp-for="Description"
                                          class="form-control form-control-sm"
                                          id="exampleFormControlTextarea1"
                                          rows="5"
                                          placeholder="Введите описание"></textarea>
                            </div>
                            <div class="form-group col-6">
                                <p class="mb-0"><kbd>Python тесты</kbd></p>
                                <textarea asp-for="PythonTest"
                                          class="form-control form-control-sm"
                                          id="exampleFormControlTextarea1"
                                          rows="8"
                                          placeholder=""></textarea>
                            </div>
                        </div>
                        <div class="mt-3 col-12 d-flex">
                            <div class="form-group col-6 pe-2">
                                <p class="mb-0"><kbd>Уровень</kbd></p>
                                <select asp-for="Level"
                                        class="form-control form-control-sm"
                                        id="exampleFormControlSelect2">
                                    @if (ViewBag.Levels != null)
                                    {
                                        @foreach (var item in ViewBag.Levels)
                                        {
                                            <option>@item.Name</option>
                                        }
                                    }
                                </select>
                                <p class="mt-2 mb-0">
                                    <kbd>Теги</kbd>
                                </p>
                                <select asp-for="Tags"
                                        multiple
                                        class="form-control form-control-sm"
                                        id="tagChoiceSelect">
                                    @if (ViewBag.Tags != null)
                                    {
                                        @foreach (var item in ViewBag.Tags)
                                        {
                                            <option>@item.Name</option>
                                        }
                                    }
                                </select>
                            </div>
                            <div class="form-group col-6">
                                <p class="mb-0"><kbd>C# тесты</kbd></p>
                                <textarea asp-for="CsharpTest"
                                          class="form-control form-control-sm"
                                          id="exampleFormControlTextarea1"
                                          rows="8"
                                          placeholder=""></textarea>
                            </div>
                        </div>
                        <div class="col-12 d-flex">
                            <button type="submit"
                                    class="mt-2 btn btn-outline-primary">
                                Создать
                            </button>
                            <p class="ms-2 pt-2">Вы можете выбрать как один язык для задания, так и два. Для публикации задания необходим хотя бы один тест на любом языке(python, c#)</p>
                        </div>
                    </form>
                </div>
                <div class="col-12 mt-4">
                    <div class="text-center">
                        <h4>Шаблон тестов Python, C#</h4>
                        <p>
                            Тест включает в себя [<kbd>Ваша функция</kbd>,
                            <kbd>Правильный ответ</kbd>]
                        </p>
                    </div>
                    <div class="container d-flex mt-2">
                        <div class="col-6 bg-white p-2 border">
                            <h5 class="col-12 bg-light rounded">Python</h5>
                            <pre data-enlighter-language="less"
                                 class="pb-0 mb-0 position-absolute">
.Tests.First().TestContent
.First().TestContent
.Tests.First().TestContent
.First().TestContent
                          </pre>
                        </div>
                        <div class="col-6 bg-white p-2 border">
                            <h5 class="col-12 bg-light rounded">C#</h5>
                            <pre data-enlighter-language="less"
                                 class="pb-0 mb-0 position-absolute">
.Tests.First().TestContent
.Tests.First().TestContent
                          </pre>
                        </div>
                    </div>
                </div>
            </div>
        </section>
    </div>
</section>

@section scripts {
    <script type="text/javascript" src="~/dist/enlighterjs.min.js"></script>
    <script type="text/javascript">
            // INIT CODE - simple page-wide initialization based on css selectors
            // - highlight all pre + code tags (CSS3 selectors)
            // - use javascript as default language
            // - use theme "enlighter" as default theme
            // - replace tabs with 2 spaces
            EnlighterJS.init("pre", "code", {
                language: "python",
                theme: "bootstrap4",
                indent: 2
            });
    </script>
}